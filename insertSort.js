function insertSort (items) {
	let last_sorted, first_unsorted
	last_sorted = 0
	first_unsorted = last_sorted + 1
	for(first_unsorted; first_unsorted<items.length;first_unsorted++) {
		for(let i=0;i<last_sorted;i++){
			if(items[first_unsorted] < )
		}
	}

}